get:
  tags:
    - company
    - no-auth
  summary: Get company by company name.
  parameters:
    - $ref: './parameters/company/companyName.yaml'
  responses:
    200:
      description: Company received.
      content:
        application/json:
          schema:
            $ref: './schemas/company.yaml'
    404:
      $ref: './responses/404.yaml'