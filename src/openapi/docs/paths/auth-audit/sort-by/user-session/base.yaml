get:
  tags:
    - auth-audit
  summary: Get user auth audits by session id.
  security:
    - cookie-auth: [ ]
    - admin-role: [ ]
  parameters:
    - $ref: './parameters/user/query-user-id.yaml'
    - $ref: './parameters/session/query-session-id.yaml'
  responses:
    200:
      description: Auth audits getting.
      content:
        application/json:
          schema:
            $ref: './schemas/auth-audit/auth-audits.yaml'
    401:
      $ref: './responses/auth/401.yaml'
    404:
      description: Audits not found.