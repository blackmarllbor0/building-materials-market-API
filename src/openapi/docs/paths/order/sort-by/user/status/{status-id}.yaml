get:
  tags:
    - order
  summary: Sort orders by status.
  security:
    - cookie-auth: [ ]
    - customer-role: [ ]
  parameters:
    - $ref: './parameters/order-status/query-order-status-id.yaml'
  responses:
    200:
      description: Orders sorted.
      content:
        application/json:
          schema:
            $ref: './schemas/order/order.yaml'
    401:
      $ref: './responses/auth/401.yaml'
    404:
      description: Orders not found.