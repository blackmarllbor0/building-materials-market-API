get:
  tags:
    - order
  summary: Get all user orders.
  security:
    - cookie-auth: [ ]
    - customer-role: [ ]
  parameters:
    - $ref: './parameters/user/user-id.yaml'
  responses:
    200:
      description: Order sorted.
      content:
        application/json:
          schema:
            $ref: './schemas/order/orders.yaml'
    401:
      $ref: './responses/auth/401.yaml'
    404:
      $ref: '../../../../responses/user/user-not-found-by-id.yaml'