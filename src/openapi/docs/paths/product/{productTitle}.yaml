get:
  tags:
    - product
    - no-auth
  summary: Get product by title.
  parameters:
    - $ref: './parameters/product/productTitle.yaml'
  responses:
    200:
      description: Product received.
      content:
        application/json:
          schema:
            $ref: './schemas/product.yaml'
    404:
      $ref: './responses/404.yaml'